# 🛠️ Guide Complet - Générateur de Projet NexusAI Module 9

## 📋 Table des Matières

1. [Introduction](#introduction)
2. [Installation](#installation)
3. [Utilisation Simple](#utilisation-simple)
4. [Utilisation Avancée](#utilisation-avancée)
5. [Format de Documentation](#format-de-documentation)
6. [Exemples Pratiques](#exemples-pratiques)
7. [Troubleshooting](#troubleshooting)
8. [FAQ](#faq)

---

## 🎯 Introduction

Le **ProjectFileGenerator** est un outil Java qui génère automatiquement l'arborescence complète d'un projet Maven/Spring Boot à partir de fichiers de documentation Markdown.

### Fonctionnalités

- ✅ Parse des fichiers Markdown contenant du code
- ✅ Détecte automatiquement les types de fichiers (Java, XML, YAML, SQL)
- ✅ Crée l'arborescence complète du projet
- ✅ Place chaque fichier au bon endroit
- ✅ Génère un rapport détaillé
- ✅ Support de multiples formats de documentation

---

## 📦 Installation

### Prérequis

```bash
# Java 21+
java -version

# Maven 3.9+ (optionnel)
mvn -version

# Git
git --version
```

### Téléchargement

```bash
# Cloner le repository
git clone https://github.com/nexusai/project-generator.git
cd project-generator

# OU télécharger uniquement le générateur
curl -O https://raw.githubusercontent.com/nexusai/tools/main/ProjectFileGenerator.java
```

### Compilation

```bash
# Créer le répertoire tools
mkdir -p tools

# Copier la classe
cp ProjectFileGenerator.java tools/

# Compiler
cd tools
javac ProjectFileGenerator.java
cd ..
```

---

## 🚀 Utilisation Simple

### Méthode 1 : Script Bash (Recommandé)

```bash
# Rendre le script exécutable
chmod +x generate-project.sh

# Générer le projet
./generate-project.sh ./my-project

# Avec des fichiers de documentation spécifiques
./generate-project.sh ./my-project ./docs/code.md
```

### Méthode 2 : Ligne de commande Java

```bash
# Syntaxe de base
java -cp tools com.nexusai.tools.ProjectFileGenerator <output-path> [doc-file]

# Exemples
java -cp tools com.nexusai.tools.ProjectFileGenerator ./nexus-moderation
java -cp tools com.nexusai.tools.ProjectFileGenerator ./nexus-moderation ./docs/module9-code.md
```

### Méthode 3 : API Java dans votre code

```java
import com.nexusai.tools.ProjectFileGenerator;

public class MyGenerator {
    public static void main(String[] args) throws Exception {
        // Créer le générateur
        ProjectFileGenerator generator = new ProjectFileGenerator("./output");
        
        // Parser et générer depuis un fichier
        generator.parseAndGenerate("./docs/code.md");
        
        // OU parser du contenu direct
        String content = """
            // FICHIER: Example.java
            package com.example;
            public class Example {}
            """;
        generator.parseContent(content);
        generator.generateFileStructure();
        
        // Générer le rapport
        System.out.println(generator.generateReport());
        generator.saveReport("./output/REPORT.txt");
    }
}
```

---

## 🎓 Utilisation Avancée

### Parser plusieurs fichiers

```java
ProjectFileGenerator generator = new ProjectFileGenerator("./output", true);

// Parser plusieurs sources
generator.parseAndGenerate("./docs/entities.md");
generator.parseAndGenerate("./docs/services.md");
generator.parseAndGenerate("./docs/controllers.md");

// Générer tout à la fin
generator.generateReport();
```

### Mode silencieux

```java
// Désactiver les logs verbeux
ProjectFileGenerator generator = new ProjectFileGenerator("./output", false);
generator.parseAndGenerate("./docs/code.md");
```

### Personnaliser l'arborescence

```java
// Le générateur normalise automatiquement les chemins
// Mais vous pouvez fournir des chemins complets dans votre documentation

// Exemples de chemins supportés:
// - "Example.java" → src/main/java/com/nexusai/moderation/Example.java
// - "controller/MyController.java" → src/main/java/com/nexusai/moderation/controller/MyController.java
// - "src/test/java/MyTest.java" → src/test/java/MyTest.java (chemin complet préservé)
```

---

## 📝 Format de Documentation

### Format Standard (Recommandé)

```markdown
# Module 9 - Code Java

## Services

// =============================================================================
// FICHIER 1: TextModerationService.java
// =============================================================================
package com.nexusai.moderation.service.moderation;

public class TextModerationService {
    // Code here
}

// =============================================================================
// FICHIER 2: DistressDetectionService.java
// =============================================================================
package com.nexusai.moderation.service.detection;

public class DistressDetectionService {
    // Code here
}

## Configuration

```yaml
# application.yml
spring:
  application:
    name: nexus-moderation
```

## Migrations SQL

-- =============================================================================
-- V1__create_tables.sql
-- =============================================================================
CREATE TABLE moderation_incidents (
    id UUID PRIMARY KEY
);
```

### Format Alternatif (Markdown Code Blocks)

````markdown
## Java Files

### TextModerationService.java

```java
package com.nexusai.moderation.service;

public class TextModerationService {
    // Code
}
```

### Configuration

```yaml
spring:
  profiles:
    active: dev
```
````

### Format Simplifié

```markdown
FICHIER: MyService.java

```java
public class MyService {}
```

FICHIER: config.yml

```yaml
server:
  port: 8080
```
```

---

## 💡 Exemples Pratiques

### Exemple 1 : Projet simple

**Documentation (simple-project.md):**

```markdown
# Simple Project

FICHIER: HelloWorld.java

```java
package com.example;

public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello World!");
    }
}
```

FICHIER: application.yml

```yaml
server:
  port: 8080
```
```

**Génération:**

```bash
java -cp tools com.nexusai.tools.ProjectFileGenerator ./simple-project ./simple-project.md
```

**Résultat:**

```
simple-project/
├── src/
│   └── main/
│       ├── java/
│       │   └── com/
│       │       └── nexusai/
│       │           └── moderation/
│       │               └── HelloWorld.java
│       └── resources/
│           └── application.yml
└── GENERATION_REPORT.txt
```

### Exemple 2 : Module complet avec tests

**Documentation (full-module.md):**

```markdown
# Full Module

// FICHIER: UserService.java
package com.nexusai.moderation.service;

public class UserService {
    public String getUser(String id) {
        return "User: " + id;
    }
}

// FICHIER: UserServiceTest.java
package com.nexusai.moderation.service;

import org.junit.jupiter.api.Test;

public class UserServiceTest {
    @Test
    void testGetUser() {
        // Test
    }
}

-- V1__create_users.sql
CREATE TABLE users (id UUID PRIMARY KEY);
```

**Génération:**

```bash
./generate-project.sh ./full-module ./full-module.md
```

**Résultat:**

```
full-module/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/nexusai/moderation/service/
│   │   │       └── UserService.java
│   │   └── resources/
│   │       └── db/migration/
│   │           └── V1__create_users.sql
│   └── test/
│       └── java/
│           └── com/nexusai/moderation/service/
│               └── UserServiceTest.java
├── pom.xml
├── docker-compose.yml
├── .gitignore
└── GENERATION_REPORT.txt
```

### Exemple 3 : Import depuis Claude Artifacts

Si vous avez reçu du code dans des artifacts Claude, voici comment l'extraire :

```bash
# 1. Copier le contenu des artifacts dans un fichier
cat > artifacts-content.md << 'EOF'
[Coller ici le contenu complet des artifacts]
EOF

# 2. Générer le projet
./generate-project.sh ./my-project ./artifacts-content.md

# 3. Vérifier
cd my-project
tree -L 3
```

---

## 🐛 Troubleshooting

### Problème : "Java not found"

**Solution :**
```bash
# Installer Java 21
# Ubuntu/Debian
sudo apt install openjdk-21-jdk

# macOS
brew install openjdk@21

# Vérifier
java -version
```

### Problème : "ClassNotFoundException"

**Solution :**
```bash
# S'assurer d'être dans le bon répertoire
cd /path/to/project-generator

# Recompiler
javac tools/ProjectFileGenerator.java

# Vérifier que la classe existe
ls -la tools/*.class
```

### Problème : "No files generated"

**Vérifications :**

1. **Format de documentation correct ?**
   ```bash
   # Vérifier la présence de marqueurs
   grep -E "(FICHIER|FILE):" your-doc.md
   ```

2. **Blocs de code bien fermés ?**
   ```bash
   # Compter les ``` (doit être pair)
   grep -c '```' your-doc.md
   ```

3. **Verbose mode activé ?**
   ```java
   // Dans le code
   ProjectFileGenerator generator = new ProjectFileGenerator(path, true); // true = verbose
   ```

### Problème : "Wrong file paths"

**Solution :**

Le générateur normalise automatiquement les chemins. Si un fichier n'est pas au bon endroit :

1. Vérifier le nom du fichier dans la documentation
2. Vérifier que le package Java correspond au chemin
3. Utiliser des chemins complets si nécessaire :

```markdown
FICHIER: src/main/java/com/custom/path/MyClass.java

```java
package com.custom.path;
public class MyClass {}
```
```

---

## ❓ FAQ

### Q: Puis-je générer dans un projet existant ?

**R:** Oui, mais attention aux écrasements. Le générateur **remplace** les fichiers existants. Recommandation :

```bash
# Sauvegarder d'abord
cp -r existing-project existing-project-backup

# Puis générer
./generate-project.sh existing-project ./docs.md
```

### Q: Comment gérer les fichiers binaires (images, JAR) ?

**R:** Le générateur ne gère que les fichiers texte. Pour les binaires :

```bash
# Après génération, copier manuellement
cp -r assets/ ./my-project/src/main/resources/static/
```

### Q: Peut-on générer que certains fichiers ?

**R:** Oui, filtrer votre documentation :

```bash
# Extraire seulement les controllers
grep -A 50 "Controller.java" full-doc.md > controllers-only.md

# Générer
./generate-project.sh ./output ./controllers-only.md
```

### Q: Comment ajouter des fichiers après génération ?

**R:** Deux options :

1. **Re-générer complètement** (recommandé pour cohérence)
2. **Ajouter manuellement** :
   ```bash
   touch ./my-project/src/main/java/NewFile.java
   ```

### Q: Le générateur supporte-t-il d'autres langages ?

**R:** Actuellement optimisé pour Java/Spring Boot, mais supporte :
- ✅ Java
- ✅ XML (Maven)
- ✅ YAML
- ✅ SQL
- ✅ Properties
- ✅ Markdown
- ⚠️ Autres langages (support basique)

Pour étendre :
```java
// Dans ProjectFileGenerator.java
private enum FileType {
    JAVA, XML, YAML, SQL,
    PYTHON,    // Ajouter ici
    JAVASCRIPT // et ici
}
```

### Q: Comment personnaliser les chemins de base ?

**R:** Modifier dans `normalizePath()` :

```java
switch (type) {
    case JAVA:
        if (!path.startsWith("src/")) {
            // Changer ici
            return "src/main/java/com/mycompany/myproject/" + path;
        }
        break;
}
```

---

## 📊 Statistiques d'Utilisation

Après génération, le rapport contient :

```
📊 PROJECT GENERATION REPORT
══════════════════════════════════════════════════════

Output path: /path/to/project
Total files: 45

Files by type:
  - JAVA           : 25 files
  - TEST_JAVA      : 8 files
  - XML            : 1 files
  - YAML           : 2 files
  - SQL            : 3 files
  - MARKDOWN       : 6 files

File tree:
📁 src/main/java/com/nexusai/moderation/
  ├── controller/
  ├── service/
  ├── model/
  └── repository/
📁 src/test/java/com/nexusai/moderation/
  └── service/
```

---

## 🎯 Cas d'Usage Réels

### Cas 1 : Onboarding nouveau développeur

```bash
# Le lead dev génère le projet
./generate-project.sh ./nexus-moderation ./complete-docs.md

# Le nouveau dev clone et lance
cd nexus-moderation
docker-compose up -d
mvn spring-boot:run
```

### Cas 2 : Prototypage rapide

```bash
# Créer la doc avec IA (Claude, ChatGPT)
# → Obtenir du code structuré

# Générer le projet
./generate-project.sh ./prototype ./ai-generated-code.md

# Tester immédiatement
cd prototype
mvn clean install
```

### Cas 3 : Synchronisation équipe

```bash
# Lead dev maintient les docs à jour
# Chaque dev regénère localement

git pull
./generate-project.sh . ./docs/latest-structure.md

# Cohérence garantie sur toute l'équipe
```

---

## 🚀 Conclusion

Le **ProjectFileGenerator** permet de gagner énormément de temps en générant automatiquement la structure de projet à partir de documentation.

**Points clés :**
- ✅ Simple à utiliser
- ✅ Flexible
- ✅ Rapide
- ✅ Fiable
- ✅ Extensible

**Pour aller plus loin :**
- Adapter le générateur à vos besoins
- Créer des templates de documentation
- Automatiser avec CI/CD
- Intégrer dans votre workflow Git

---

**Besoin d'aide ?**
- 📧 Email: tools-support@nexusai.com
- 💬 Slack: #project-generator
- 📖 Docs: https://docs.nexusai.com/tools/generator

**Bon développement ! 🎉**
