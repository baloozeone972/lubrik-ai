package com.nexusai.core.enums;

/**
 * Role of the message sender (for LLM context).
 */
public enum MessageRole {
    SYSTEM,     // System prompt
    USER,       // User message
    ASSISTANT   // AI response
}
